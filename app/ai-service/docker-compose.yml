services:
  # Celery Worker
  # celery-worker:
  #   build:
  #     context: .
  #     dockerfile: Dockerfile
  #     target: celery
  #   container_name: screening-celery
  #   env_file:
  #     - .env
  #   environment:
  #     PYTHONUNBUFFERED: 1
  #     PYTHONDONTWRITEBYTECODE: 1
  #     PYTHONPATH: /app
  #     # Use redis-server consistently
  #     REDIS_URL: redis://redis-server:6379/0
  #     CELERY_BROKER_URL: redis://redis-server:6379/0
  #     CELERY_RESULT_BACKEND: redis://redis-server:6379/0
  #   volumes:
  #     - ./data:/app/data
  #     - ./keys:/app/keys
  #     - ./uploads:/app/uploads
  #   develop:
  #     watch:
  #       - action: sync
  #         path: .
  #         target: /app
  #         ignore:
  #           - .venv/
  #           - data/
  #           - uploads/
  #           - keys/
  #       - action: rebuild
  #         path: ./pyproject.toml
  #   depends_on:
  #     - redis
  #   networks:
  #     - backend
  #   restart: unless-stopped

  # # Celery Flower
  # flower:
  #   build:
  #     context: .
  #     dockerfile: Dockerfile
  #     target: flower
  #   container_name: screening-flower
  #   ports:
  #     - "5555:5555"
  #   environment:
  #     # Use redis-server consistently
  #     CELERY_BROKER_URL: redis://redis-server:6379/0
  #     CELERY_RESULT_BACKEND: redis://redis-server:6379/0
  #   depends_on:
  #     - redis
  #     - celery-worker
  #   networks:
  #     - backend
  #   restart: unless-stopped

include:
  - ../../config/compose.yml # Postgres & Redis Service
  - ./docker/compose.yml # AI SERVICE 
  - ../hrms-app/docker/compose.yml # HRMS APP Service
