# AI-HRMS

# Development
development with docker is not yet supported
docker compose -f docker-compose.yml -f docker-compose.development.yml up --build


# Production
production with docker is not yet supported
docker compose -f docker-compose.yml -f docker-compose.production.yml up -d


# Future Development
- Make All queue job in HRMS-APP and ai-service can be tracked in frontend